import ReactDOM from 'react-dom/client'
import FocusScreen from './components/04-useRef/FocusScreen.tsx'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
    <FocusScreen />
)
