import ReactDOM from 'react-dom/client'
import Memorize from './components/06-memos/Memorize.tsx'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
    // <React.StrictMode>
        <Memorize />
    // </React.StrictMode>
)
